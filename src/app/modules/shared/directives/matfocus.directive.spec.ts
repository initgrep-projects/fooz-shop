import { MatfocusDirective } from './matfocus.directive';

describe('MatfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MatfocusDirective();
    expect(directive).toBeTruthy();
  });
});
